import styled from "styled-components";

export const TaxBracket = styled.div`
  padding-top: 7px;
  label {
    font-weight: bold;
    font-size: 11px !important;
    line-height: 13px !important;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--text);
  }
`;